services:
  - type: web
    name: devtrace-ai-backend
    runtime: go
    rootDir: devtrace-ai-backend
    buildCommand: go build -o server .
    startCommand: ./server
    plan: free
    envVars:
      - key: PORT
        value: "10000"
